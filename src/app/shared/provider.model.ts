export class Provider {
    constructor(public ais: string[], public country: string,
        public country_full: string,
        public name: string,
        public pis: string[],
        public provider: string,
        ) {}
    }
